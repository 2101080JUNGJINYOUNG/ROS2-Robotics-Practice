# 15_16_17_Cpp고급기능 과제

> ⬆ [ROS2-Robotics-Practice로 돌아가기](https://github.com/2101080JUNGJINYOUNG/ROS2-Robotics-Practice)  ·  📚 [공부 목차](../README.md)

아래는 실제로 제출했던 15장, 16장, 17장 과제 원문입니다. 정답을 보기 전에 먼저 스스로 답을 작성하거나 직접 실습해보세요.

## 15_Cpp고급기능-1

### 1. 실습과제1

일반 함수와 람다식의 차이를 자세히 설명하시오. 일반 함수로 정의하는 대신에 람다식을 사용할 때 장점은 무엇인가?

<details>
<summary>정답 보기</summary>

일반 함수와 람다식의 가장 근본적인 차이는 익명성과 정의 위치에 있습니다. 일반 함수는 고유한 이름을 가지며 특정 네임스페이스나 클래스 범위에 독립적으로 정의되지만, 람다식은 이름이 없는 익명 함수로서 함수를 호출하거나 변수에 대입하는 코드 위치에 직접 인라인(inline)으로 작성됩니다. 이러한 인라인 정의 방식은 STL 알고리즘에 전달할 조건자(predicate)나 간단한 콜백 함수처럼 일회성으로 사용되는 짧은 로직을 구현할 때, 코드의 다른 부분에 별도의 함수를 선언하고 정의하는 번거로움을 없애주어 코드의 간결성을 높이고 참조의 지역성을 강화합니다. 즉, 함수의 기능과 사용처가 한눈에 들어와 가독성과 유지보수성이 크게 향상됩니다. 더 나아가 람다식의 가장 강력한 장점은 캡쳐(capture) 기능에 있습니다. 람다식은 대괄호 []를 이용해 자신이 선언된 외부 함수의 지역 변수들을 값이나 참조로 '포착'하여 함수 내부에서 사용할 수 있으며, 이는 일반 함수가 매개변수나 전역 변수를 통해서만 외부 데이터에 접근할 수 있는 것과 대조되는 특징으로, 복잡한 클래스나 구조체를 정의하지 않고도 특정 상태를 기억하고 동작하는 상태 저장(stateful) 함수 객체를 매우 손쉽게 만들 수 있게 해줍니다.

</details>

### 2. 실습과제2

23페이지 예제처럼 함수에 인자로 람다식을 사용하는 예제를 인터넷에서 찾아서 실행해보고 소스코드를 자세히 설명하시오.

<details>
<summary>정답 보기</summary>

```cpp
// std::function, std::cout, std::string 사용을 위한 헤더 파일 포함
#include <functional>
#include <iostream>
#include <string>

// 1. 일반(named) 함수 정의
int some_func1(const std::string& a) {
    std::cout << "Func1 호출! " << a << std::endl;
    return 0;
}

// 2. 함수 호출 연산자 operator()를 오버로딩한 '함수 객체(Functor)' 정의
struct S {
    void operator()(char c) { std::cout << "Func2 호출! " << c << std::endl; }
};

int main() {
    /*
     * std::function: 호출 가능한 모든 것을 감싸는 템플릿 클래스.
     * 일반 함수, 함수 객체, 람다식 등 다양한 형태의 호출 가능한 개체를
     * 동일한 방식으로 저장하고 사용할 수 있게 해준다.
     */

     // 'f1'에 일반 함수(some_func1)의 주소를 저장
    std::function<int(const std::string&)> f1 = some_func1;

    // 'f2'에 함수 객체(S의 인스턴스)를 저장
    std::function<void(char)> f2 = S();

    // 'f3'에 람다 표현식을 저장
    std::function<void()> f3 = []() { std::cout << "Func3 호출! " << std::endl; };

    // std::function 객체에 저장된 callable들을 동일한 문법으로 호출
    f1("hello"); // some_func1 호출
    f2('c');     // S() 객체의 operator() 호출
    f3();        // 람다식 호출
}
```

**콘솔 출력**

```
Func1 호출! hello
Func2 호출! c
Func3 호출!
```

`std::function`은 함수 포인터, 함수 객체(Functor), 람다식처럼 서로 다른 종류의 "호출 가능한 대상"을 동일한 타입으로 감싸서 저장할 수 있는 템플릿 클래스입니다. 위 예제에서는 일반 함수 `some_func1`의 주소를 `f1`에, `operator()`를 오버로딩한 구조체 `S`의 인스턴스를 `f2`에, 캡쳐 없는 람다식을 `f3`에 각각 저장한 뒤 `f1(...)`, `f2(...)`, `f3()`처럼 완전히 동일한 함수 호출 문법으로 실행합니다. 이처럼 서로 다른 형태의 콜러블을 동일한 인터페이스로 다룰 수 있다는 점이 `std::function`의 핵심 장점입니다.

</details>

### 3. 실습과제3

함수 포인터와 std::function 객체의 차이를 자세히 설명하시오.

<details>
<summary>정답 보기</summary>

함수 포인터와 std::function의 가장 큰 차이는 저장할 수 있는 대상의 범위와 안전성에 있습니다. 함수 포인터는 C-스타일의 순수한 포인터로, 시그니처가 일치하는 일반(전역) 함수의 메모리 주소만 저장할 수 있는 반면, std::function은 일반 함수뿐만 아니라, 함수 객체(Functor)와 캡쳐 기능이 있는 람다 표현식 등 호출 가능한 모든 것을 담을 수 있는 유연한 클래스 템플릿 객체입니다. 이러한 유연성 덕분에 std::function은 타입에 안전하며 포인터 사용으로 인한 오류를 방지해 주지만, 내부적으로 추가적인 메커니즘을 사용하므로 순수한 함수 포인터에 비해 약간의 성능 오버헤드가 발생할 수 있습니다. 결론적으로, 함수 포인터는 가볍고 빠르지만 기능이 제한적인 반면, std::function은 약간의 비용을 감수하고 훨씬 뛰어난 다형성과 안정성을 제공하는 현대적인 C++의 대안입니다.

</details>

### 4. 실습과제4

23페이지 예제처럼 인터넷에서 std::function 클래스를 함수에 매개변수에 사용한 예제를 찾아서 실행해보고 소스코드를 자세히 설명하시오.

<details>
<summary>정답 보기</summary>

```cpp
#include <iostream>
#include <functional> // std::function을 사용하기 위해 반드시 포함해야 합니다.
#include <string>

// 계산을 수행하고 결과를 출력하는 고차 함수(Higher-order function)
// 세 번째 인자로 어떠한 연산을 수행할지 'std::function' 객체로 전달받습니다.
void calculate(int a, int b, std::function<int(int, int)> operation) {
    int result = operation(a, b); // 전달받은 연산(operation)을 실행합니다.
    std::cout << "결과: " << result << std::endl;
}

// 1. 일반 함수: 덧셈
int add(int a, int b) {
    return a + b;
}

// 2. 일반 함수: 뺄셈
int subtract(int a, int b) {
    return a - b;
}

int main() {
    int a = 10, b = 5;

    std::cout << a << " + " << b << " 연산" << std::endl;
    calculate(a, b, add); // 'add' 함수를 인자로 전달

    std::cout << a << " - " << b << " 연산" << std::endl;
    calculate(a, b, subtract); // 'subtract' 함수를 인자로 전달

    std::cout << a << " * " << b << " 연산" << std::endl;
    // 3. 람다 표현식: 곱셈
    calculate(a, b, [](int x, int y) { return x * y; }); // 곱셈 람다를 인자로 전달

    return 0;
}
```

**콘솔 출력**

```
10 + 5 연산
결과: 15
10 - 5 연산
결과: 5
10 * 5 연산
결과: 50
```

calculate 함수는 두 정수와 함께 std::function<int(int, int)> 타입의 operation 객체를 매개변수로 받는데, 이 operation 객체는 두 개의 int를 인자로 받아 int를 반환하는 모든 호출 가능한 것을 담을 수 있습니다. 따라서 main 함수에서는 미리 정의된 add와 같은 일반 함수 포인터를 전달할 수도 있고, 곱셈 연산처럼 별도의 함수 정의 없이 [](int x, int y){ return x*y; }와 같은 람다 표현식을 즉석에서 작성하여 전달할 수도 있습니다. calculate 함수는 내부적으로 operation(a, b) 코드를 통해 어떤 기능이 전달되었든 동일한 방식으로 실행하여 결과를 얻으며, 이처럼 std::function을 통해 수행할 연산 자체를 파라미터로 주입하는 방식은 단 하나의 함수를 매우 유연하고 확장성 있게 만드는 좋은 방법을 보여줍니다.

</details>

## 16_Cpp고급기능-2

### 1. 실습과제1

아래는 std::bind 함수의 선언이다. 여기서 매개변수 선언에서 &&의 의미를 설명하라.

```cpp
template< class F, class... Args >
/*unspecified*/ bind(F&& f, Args&&... args);
```

<details>
<summary>정답 보기</summary>

**1. 우측값(R-value) 참조**: &&는 기본적으로 임시 객체나 리터럴 상수와 같은 "우측값"만 참조할 수 있는 변수를 선언할 때 사용됩니다. 예를 들어 `int&& r = 10;`은 가능하지만, 변수를 참조하는 `int i = 5; int&& r = i;`는 컴파일 오류가 발생합니다. 이는 임시 객체의 소유권을 "이동(move)"하여 불필요한 복사를 피하고 성능을 향상시키는 이동 의미론(Move Semantics)의 핵심 요소입니다.

**2. 전달 참조(Forwarding Reference)**: std::bind의 선언에서처럼 F&& f와 Args&&... args는 템플릿 타입 추론과 결합되어 전달 참조로 동작합니다.

- 함수에 좌측값(L-value, 변수 등)이 전달되면, 해당 매개변수는 좌측값 참조(&) 타입으로 추론됩니다.
- 함수에 우측값(R-value, 상수, 임시 객체 등)이 전달되면, 해당 매개변수는 우측값 참조(&&) 타입으로 추론됩니다.

결론적으로, std::bind 선언에서 &&는 함수 f와 인자 args가 좌측값이든 우측값이든 상관없이 모두 받을 수 있게 해주는 특별한 문법입니다. 이를 통해 std::bind는 전달받은 인자들을 원래의 값 속성(좌측값 또는 우측값) 그대로 내부 함수에 전달(perfect forwarding)하여, 불필요한 복사를 방지하고 효율성을 극대화할 수 있습니다.

</details>

### 2. 실습과제2

인터넷에서 함수의 인자로 std::bind 함수를 사용한 예제를 찾아서 실행해보고 소스코드를 자세히 설명하시오.

<details>
<summary>정답 보기</summary>

```cpp
#include <iostream>
#include <vector>
#include <functional>
#include <algorithm>

// 숫자가 특정 임계값보다 큰지 확인하는 일반 함수
bool isGreaterThan(int value, int threshold) {
    return value > threshold;
}

int main() {
    // 테스트용 정수 벡터 생성
    std::vector<int> numbers = {10, 55, 23, 8, 91, 44, 78};

    // std::bind를 사용하여 isGreaterThan 함수의 두 번째 인자를 50으로 고정
    // placeholders::_1은 count_if가 전달할 첫 번째 인자(벡터의 각 요소)를 의미
    auto isGreaterThan50 = std::bind(isGreaterThan, std::placeholders::_1, 50);

    // std::count_if는 세 번째 인자로 받은 함수(isGreaterThan50)를 벡터의
    // 모든 요소에 대해 호출하고, 그 결과가 true인 요소의 개수를 반환
    int count = std::count_if(numbers.begin(), numbers.end(), isGreaterThan50);

    // 결과 출력
    std::cout << "50보다 큰 숫자의 개수: " << count << std::endl;

    return 0;
}
```

`std::bind`는 기존 함수의 일부 인자를 미리 고정하여 새로운 호출 가능한 객체(callable)를 만들어주는 함수입니다. 위 예제에서는 두 개의 인자를 받는 `isGreaterThan(value, threshold)` 함수의 두 번째 인자 `threshold`를 50으로 고정하고, 첫 번째 인자 자리에는 `std::placeholders::_1`을 넣어 `isGreaterThan50`이라는 새로운 콜러블을 만듭니다. 이 `_1`은 나중에 `isGreaterThan50`이 실제로 호출될 때 전달되는 첫 번째 인자로 채워지는 자리표시자입니다. 이렇게 만들어진 `isGreaterThan50`을 `std::count_if`에 넘기면, 벡터의 각 원소가 순서대로 `_1` 자리에 대입되어 `isGreaterThan(원소, 50)`이 호출되고, 그 결과가 true인 원소의 개수를 세게 됩니다.

</details>

### 3. 실습과제3

인터넷에서 std::chrono_literals 을 사용한 예제를 찾아서 실행해보고 소스코드를 자세히 설명하시오.

<details>
<summary>정답 보기</summary>

```cpp
#include <iostream>
#include <chrono>
#include <thread>

// using namespace std::chrono_literals 지시문을 사용하여
// ms, s 등의 시간 리터럴을 바로 사용할 수 있도록 함
using namespace std::chrono_literals;

int main() {
    // 1. 밀리초(ms) 리터럴 사용
    std::cout << "1. 500ms 동안 대기합니다..." << std::endl;
    // 500ms는 operator""ms(500)으로 변환되어 std::chrono::milliseconds 객체를 생성
    std::this_thread::sleep_for(500ms);
    std::cout << "   대기 완료!" << std::endl;

    // 2. 초(s) 리터럴 사용
    std::cout << "2. 2초 동안 대기합니다..." << std::endl;
    // 2s는 operator""s(2)로 변환되어 std::chrono::seconds 객체를 생성
    std::this_thread::sleep_for(2s);
    std::cout << "   대기 완료!" << std::endl;

    // 3. 리터럴을 사용한 시간 계산
    auto total_sleep_time = 1s + 250ms;
    std::cout << "3. 총 " << total_sleep_time.count() << "밀리초 동안 대기합니다..." << std::endl;
    std::this_thread::sleep_for(total_sleep_time);
    std::cout << "   대기 완료!" << std::endl;

    return 0;
}
```

`std::chrono_literals`는 `500ms`, `2s`처럼 숫자 뒤에 시간 단위를 바로 붙여서 `std::chrono::milliseconds`, `std::chrono::seconds` 같은 객체를 직관적으로 만들 수 있게 해주는 사용자 정의 리터럴 모음입니다. 위 예제는 `using namespace std::chrono_literals;`를 선언한 뒤 `500ms`, `2s` 같은 리터럴을 `std::this_thread::sleep_for()`에 바로 전달해 각각 500밀리초, 2초 동안 스레드를 대기시킵니다. 또한 `1s + 250ms`처럼 서로 다른 시간 단위의 리터럴끼리도 연산이 가능하며, 결과는 자동으로 공통 단위(밀리초)로 변환되어 `total_sleep_time.count()`로 그 값을 확인할 수 있습니다.

</details>

## 17_Cpp고급기능-3

### 1. 실습과제1

다음 코드를 스마트 포인터를 이용한 코드로 변경하시오.

```cpp
#include <iostream>
using namespace std;
int main()
{
    int* ptr1 = new int(5);
    cout << *ptr1 << endl;

    int* ptr2 = ptr1;
    *ptr2 = 10;
    cout << *ptr2 << endl;

    delete ptr1;

    return 0;
}
```

<details>
<summary>정답 보기</summary>

```cpp
#include <memory>   // 스마트 포인터(특히 unique_ptr)를 사용하기 위한 헤더 파일
#include <iostream> // 표준 입출력 스트림을 사용하기 위한 헤더 파일
using namespace std; // std 네임스페이스 내의 이름들을 간단히 사용하기 위함
int main()
{
    auto ptr1 = make_unique<int>(5); // make_unique로 int형 값을 5로 초기화한 unique_ptr 생성 (소유권은 ptr1에 있음)
    cout << *ptr1 << endl; // ptr1이 가리키는 메모리의 값을 출력 (현재 5)
    auto ptr2 = move(ptr1); // move를 이용해 ptr1의 소유권을 ptr2로 이동 (ptr1은 이제 nullptr 상태)
    *ptr2 = 10; // ptr2가 소유한 메모리의 값을 10으로 변경
    cout << *ptr2 << endl; // 변경된 값(10)을 출력
    return 0; // main 함수 종료, ptr2가 소멸되며 가리키던 메모리 자동 해제
}
```

**콘솔 출력**

```
5
10
```

기존 코드는 `new`로 할당한 메모리를 `ptr1`이 가리키다가 `ptr2 = ptr1`로 같은 주소를 하나 더 가리키게 한 뒤 `delete ptr1`만 호출하는 구조라, 소유권이 불분명하고 이중 해제나 메모리 누수의 위험이 있습니다. 스마트 포인터 버전에서는 `make_unique<int>(5)`로 메모리를 할당함과 동시에 소유권을 가진 `unique_ptr`인 `ptr1`을 만들고, `unique_ptr`는 복사가 금지되어 있으므로 `move(ptr1)`을 사용해 소유권을 `ptr2`로 완전히 이전합니다. 이 시점부터 `ptr1`은 더 이상 메모리를 가리키지 않는 빈(nullptr) 상태가 되고, `ptr2`가 유일한 소유자가 되어 값을 수정할 수 있습니다. `delete`를 명시적으로 호출하지 않아도 `main` 함수가 끝나며 `ptr2`가 소멸될 때 소멸자가 자동으로 메모리를 해제해주므로, 메모리 누수나 이중 해제 문제에서 안전합니다.

</details>

### 2. 실습과제2

다음 코드를 스마트 포인터를 이용한 코드로 변경하시오.

```cpp
#include <memory>
using namespace std;
class CTest
{
    int test;
public:
    CTest(int value) { test = value;  cout << "생성자호출" << endl; }
    ~CTest() { cout << "소멸자호출" << endl; }
    int GetTest() { return test; }
    void SetTest(int value) { test = value; }
};
```

<details>
<summary>정답 보기</summary>

```cpp
#include <memory>   // 스마트 포인터(예: shared_ptr, unique_ptr) 관련 기능을 사용하기 위한 헤더
#include <iostream> // 입출력 스트림(cin, cout 등)을 사용하기 위한 헤더
using namespace std; // std 네임스페이스에 있는 이름들을 직접 사용하기 위함
class CTest
{
    int test; // 클래스 내부에서 사용할 정수형 멤버 변수
public:
    CTest(int value) { test = value; cout << "생성자호출" << endl; } // 생성자: 객체 생성 시 값으로 멤버를 초기화하고 메시지 출력
    ~CTest() { cout << "소멸자호출" << endl; } // 소멸자: 객체가 파괴될 때 호출되어 메시지 출력
    int GetTest() { return test; } // 멤버 변수 test의 값을 반환하는 접근자 함수
    void SetTest(int value) { test = value; } // 멤버 변수 test의 값을 설정하는 변경자 함수
};

int main()
{
    auto ptr1 = make_shared<CTest>(100); // make_shared로 CTest 객체를 힙에 생성하고 shared_ptr(ptr1)에 저장 (참조 카운트 1)
    cout << ptr1->GetTest() << endl; // ptr1이 가리키는 객체의 GetTest() 호출 결과 출력 (현재 100)
    auto ptr2 = ptr1; // ptr1을 ptr2에 복사: 두 shared_ptr가 같은 객체를 가리키며 참조 카운트가 증가(이제 2)
    ptr2->SetTest(200); // ptr2를 통해 객체의 test 값을 200으로 변경 (객체는 하나뿐)
    cout << ptr2->GetTest() << endl; // ptr2가 가리키는 객체의 현재 값(200)을 출력
    return 0; // main 종료, 지역변수들이 소멸되며 shared_ptr의 참조 카운트가 감소 -> 마지막 소멸 시 객체 소멸자 호출
} // 프로그램 종료 지점 (모든 리소스 해제 완료)
```

**콘솔 출력**

```
생성자호출
100
200
소멸자호출
```

기존 코드는 `CTest` 클래스 정의만 있고 스마트 포인터를 쓰지 않는 상태였으므로, 여러 포인터가 같은 객체를 공유해서 참조할 수 있는 `shared_ptr`로 바꾸는 것이 적절합니다. `make_shared<CTest>(100)`으로 객체를 생성하면 참조 카운트가 1인 `shared_ptr`(`ptr1`)이 만들어지고, `ptr2 = ptr1`처럼 복사가 일어날 때마다(‘unique_ptr’와 달리 복사가 허용됨) 참조 카운트가 1씩 증가합니다. `ptr1`과 `ptr2`는 같은 객체를 가리키므로 `ptr2->SetTest(200)`으로 값을 바꾸면 `ptr1`을 통해서도 바뀐 값이 보이게 됩니다. `main` 함수가 끝나 `ptr1`, `ptr2`가 모두 소멸될 때 참조 카운트가 0이 되는 시점에 비로소 `CTest`의 소멸자가 호출되어 객체가 해제되므로, 콘솔에는 생성자 호출이 한 번, 소멸자 호출도 마지막에 단 한 번만 출력됩니다.

</details>

---

⬆ [ROS2-Robotics-Practice로 돌아가기](https://github.com/2101080JUNGJINYOUNG/ROS2-Robotics-Practice)  ·  ➡ [다음 장: 18\~20장. ROS2 rclcpp](../18_19_20_ROS2_rclcpp/README.md)  ·  📚 [공부 목차](../README.md)
