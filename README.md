# ROS2-Robotics-Practice

ROS2(Foxy)와 로봇 SW 수업에서 진행한 실습 과제들을 정리한 저장소입니다. ROS2의 기본 개념(DDS, 토픽, 인터페이스, 파일시스템)부터 시작해서, C++ 클라이언트 라이브러리(rclcpp)로 직접 노드를 작성하고, 최종적으로 Jetson Nano 보드 + Dynamixel 모터 + 카메라를 이용한 실제 로봇 실습(키보드 원격 조작, 카메라 영상 기반 라인 검출)까지 진행한 과정을 담고 있습니다.

## 배운 것 / 깨달은 것 / 성취한 것

ROS1이 Master를 통해 노드를 중앙집중적으로 관리하는 것과 달리 ROS2는 DDS 기반의 분산형 통신으로 Master 없이 노드들이 서로를 찾는다는 구조적 차이를 과제로 정리해보고, turtlesim에서 `ros2 topic/service/action/node list`와 `ros2 interface show`로 토픽·서비스·액션의 개념과 인터페이스 구조를 직접 조회하고 `ros2 topic pub`으로 거북이를 직접 움직여봤습니다. `rqt_graph`로 노드-토픽 연결을 시각화해보면서, 추상적으로만 알던 pub/sub 구조가 실제로 어떻게 동작하는지 이해할 수 있었습니다.

rclcpp로 직접 퍼블리셔/서브스크라이버 노드를 작성하면서는, 콜백 함수를 어떻게 구성하느냐(일반 함수, 람다, `std::bind`)에 따라 코드 구조가 크게 달라진다는 것을 체감했고, C++ 고급 문법(스마트 포인터, `std::function`, 시간 리터럴)이 실제 ROS2 코드베이스 곳곳에서 왜 필요한지도 이해하게 되었습니다.

가장 크게 성장했다고 느낀 부분은 후반부의 두 가지 실습입니다. 하나는 Jetson Nano와 Dynamixel 모터를 연결해, 키보드 입력(전진·후진·좌우 회전)을 받아 속도 값을 토픽으로 발행하는 노드와 이를 구독해 실제로 모터를 구동하는 노드를 분리해서 설계한 원격 조작(teleoperation) 실습이고, 다른 하나는 카메라 영상을 ROS2 토픽으로 발행하는 노드(`pubpub`)와 그 영상을 구독해 OpenCV로 이진화·블롭 검출을 수행해 라인의 위치를 계산하는 노드(`subsub`)를 분리해서 설계한 라인 검출 실습입니다. 두 실습은 서로 다른 프로젝트였지만, 각각을 통해 "노드 하나가 입력을 받아 토픽으로 발행하면, 이를 구독한 다른 노드가 실제 동작(모터 구동, 영상 처리)을 수행한다"는 것을 직접 확인하면서, 여러 개의 독립적인 노드가 네트워크를 통해 협력해서 동작하는 ROS의 pub/sub 핵심 철학을 실제로 체감할 수 있었습니다.

## 실습 목록

- 📁 [01_ROS와_ROS2](./01_ROS와_ROS2/) — 메타운영체제, 미들웨어, DDS 등 ROS 기본 개념과 자율주행 응용 사례 조사
- 📁 [02_ROS2개발환경구축](./02_ROS2개발환경구축/) — ROS2(Foxy) 환경 구축, talker/listener 실행, colcon 빌드, 환경변수 설정
- 📁 [03_04_05_ROS2특징과DDS](./03_04_05_ROS2특징과DDS/) — ROS1 vs ROS2 구조 비교, DDS 동작 원리, turtlesim으로 패키지·노드 실습
- 📁 [06_ROS2토픽](./06_ROS2토픽/) — Twist 메시지 구조, `ros2 topic echo/bw/hz/pub`으로 토픽 통신 직접 실습
- 📁 [10_ROS2인터페이스](./10_ROS2인터페이스/) — 메시지·토픽·서비스·액션·인터페이스 용어 정리, `ros2 interface show`로 실제 구조 조회
- 📁 [11_CMake사용법](./11_CMake사용법/) — CMake 빌드 3단계, 직접 프로젝트 작성(adder, OpenCV 이미지 처리) 및 빌드
- 📁 [13_ROS2파일시스템](./13_ROS2파일시스템/) — 빌드시스템 vs 빌드툴, `ros2 pkg create`로 패키지 생성, 빌드 산출물 구조 이해
- 📁 [15_16_17_Cpp고급기능](./15_16_17_Cpp고급기능/) — 람다/`std::function`/`std::bind`/스마트 포인터 등 ROS2 코드에 자주 쓰이는 C++ 고급 문법
- 📁 [18_19_20_ROS2_rclcpp](./18_19_20_ROS2_rclcpp/) — rclcpp로 퍼블리셔/서브스크라이버 노드를 3회차에 걸쳐 직접 작성
- 📁 [22_실습로봇소개](./22_실습로봇소개/) — 실습에 사용한 Jetson Nano, IMX219 카메라, Dynamixel, U2D2 등 로봇 하드웨어 조사
- 📁 [23_ROS2카메라](./23_ROS2카메라/) — 로봇에 연결된 장치들의 네트워크 설정 확인
- 📁 [24_ROS2다이내믹셀](./24_ROS2다이내믹셀/) — Dynamixel 모터를 ROS2 노드로 직접 제어, 키보드 입력 기반 원격 조작(teleoperation)까지 확장
- 📁 [25_ROS2라인검출](./25_ROS2라인검출/) — 카메라 영상을 이용한 라인 검출(Line Tracer) 노드 구현, 데모 영상 포함
- 📁 [26_네트워크와개발환경구축](./26_네트워크와개발환경구축/) — WSL2, SSH 원격 접속, TCP/IP, ping을 통한 네트워크 기초 실습

## 참고

- 개발 환경: ROS2 Foxy, Ubuntu, Jetson Nano
- 원본 자료는 수업 중 배포된 PDF 강의자료/과제와, 실습 중 작성한 소스 코드를 기반으로 재구성했습니다.
- 일부 챕터(rclcpp, 다이내믹셀, 카메라, 네트워크 설정)의 원본 README에는 다른 GitHub 계정에 업로드된 이미지가 포함되어 있었으나, 해당 이미지 CDN에 현재 접근이 되지 않아 이번 정리에는 포함하지 못했습니다. 코드와 텍스트 설명은 모두 원본 그대로 보존했습니다.
